"""Notifuse MCP server package.

Exposes the complete Notifuse HTTP API as Model Context Protocol tools so that
every Notifuse operation can be driven from a chat client (Cursor, Claude
Desktop, etc.).
"""

__version__ = "0.1.0"
